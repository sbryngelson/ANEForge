"""Productized native-ANE netplist bridges — the self-contained dispatch closure
promoted out of the reverse-engineering corpus. Each *_fused module authors a
Type=<Layer> netplist and runs it Path-A; _netplist.py is the shared netplist author."""
