# API reference

This reference is generated from the package's own docstrings. The op surface below is
the overview from `aneforge`; the sub-pages document the graph and operators, the
compile and optimization entry points, training, the pretrained loaders, and the
applied-math toolkits.

::: aneforge
    options:
      members: false
      show_root_heading: false
      show_root_toc_entry: false
